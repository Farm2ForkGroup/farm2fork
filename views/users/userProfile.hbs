<h1>Profile</h1>
<div>
  <div class="text-center">
    <img class="rounded-circle" src="{{currentUser.image}}" alt="User" height="60">
    <h2>{{currentUser.username}}</h2>
    <h3>{{currentUser.email}}</h3>

    <a href="/products/new" class="btn btn-primary">Create a new ad</a>
  </div>
</div>